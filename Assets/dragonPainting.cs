using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class dragonPainting : MonoBehaviour
{
    // Start is called before the first frame update
    public int paintingNumber;


    // Update is called once per frame
    void Update()
    {
        
    }
}
